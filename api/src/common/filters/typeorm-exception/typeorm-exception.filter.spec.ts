import { TypeormExceptionFilterTsFilter } from './typeorm-exception.filter';

describe('TypeormExceptionFilterTsFilter', () => {
  it('should be defined', () => {
    expect(new TypeormExceptionFilterTsFilter()).toBeDefined();
  });
});
